HFT_ULTRA_LATENCY_PROJECT_V1.0: THE MASTER BLUEPRINT

1. The Physical Infrastructure (The "Zero-Distance" Server)
To beat the rest of the network, your code must physically live next to Polymarket's centralized off-chain matching engine (the CLOB).Server Type: Dedicated Bare-Metal Server (Do not use shared VPS like DigitalOcean or AWS EC2 instances).Location: AWS US-East-1 (N. Virginia) or a premium Frankfurt data center.Hardware Requirements: Minimum 32-core CPU, 128GB RAM, and 2TB NVMe SSD for fast state reading.Network Interface: A Mellanox or Solarflare Network Interface Card (NIC) with kernel-bypass capabilities. This allows your trading data to skip the Linux operating system and go straight into your Rust application, saving precious milliseconds.The Node: Run an Erigon Polygon full node directly on this machine.

2. The Software Stack (The Rust Engine)
Python and Node.js are too slow for this meta. Your entire execution engine must be written in Rust.The Core Library: Use the highly optimized open-source Rust crate polyfill-rs or the official polymarket-sdk. These utilize SIMD (Single Instruction, Multiple Data) JSON parsing to process the order book 20% faster than standard clients.The Connection: Establish a persistent WebSocket stream directly to Polymarket's CLOB via wss://ws-live-data.polymarket.com.State Management: Keep a local, in-memory copy of the order book using a Rust BTreeMap. This allows you to calculate spreads in nanoseconds without asking the API for the current price.

3. The "Shadow Maker" 
Execution ProtocolThis is the exact logic your bot will use to exploit RN1's trades without paying the new 0.75% taker fees.The Sniffer: Your Rust engine listens to the WebSocket feed, filtering strictly for the wallet address of RN1.The Trigger: The millisecond RN1 broadcasts a new EIP-712 signed limit order (e.g., placing $50,000 on "Yes" for a specific CS:GO match at $0.65), your bot intercepts the data before it hits the blockchain.The Sizing Math: You cannot copy their massive size, or you will be trapped in the order book. Your bot must calculate a micro-allocation using a strict multiplier: $Your\_Order\_Size = RN1\_Size \times 0.02$The Placement: Your bot instantly generates an identical limit order at $0.65 and signs it with your wallet's private key.Order Type: You must append the Post-Only flag to your order. This guarantees your order will only be added to the book as a Maker. If the market has already moved and your order would execute as a Taker, the exchange instantly drops it, saving you from the 0.75% fee.

4. The 3-Second In-Play Failsafe
Polymarket enforces a mandatory 3-second delay on orders placed during a live sporting event to prevent television front-running.The Cancellation Loop: Because of this delay, the score of the game might change while your order is waiting in the queue.The Radar: While your order is in its 3-second countdown, your Rust engine must aggressively poll the live sports data API.The Abort Sequence: If the odds shift against you by more than 2% during that 3-second window, your bot must instantly fire a CancelOrder request to the CLOB before your trade goes live.This is the exact architecture required to survive and profit in the 2026 Polymarket sports meta.